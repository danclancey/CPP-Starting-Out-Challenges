#ifndef CHARCONVERTER_H
#define CHARCONVERTER_H 

#include <string>

using namespace std;

class CharConverter { 
    public:
        string uppercase(const string& str);
        string properWords(const string& str);
};

#endif
